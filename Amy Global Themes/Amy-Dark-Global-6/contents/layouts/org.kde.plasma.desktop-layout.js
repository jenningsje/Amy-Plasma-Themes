var desktopsArray = desktopsForActivity(currentActivity());
for (var j = 0; j < desktopsArray.length; j++) {
    // Example: only set some other config, but don't touch wallpaper
    // desktopsArray[j].someOtherProperty = 'something';
}
